pipeline {
    agent any

    triggers {webhook('https://github.com/Bhargavi-lakamsani/practice-repo.git')}

    stages {
        stage('Stage-1') {
              steps {
                sh 'touch tomcat'
            }
        }

        stage('Stage-2') {
            steps {
                sh 'touch master'
            }
        }
    }
}
